// HelmCode Demo slice — safe to delete after onboarding
package {{basePackage}}.facade.blacklist.command;

import com.mycm.common.model.base.BaseRequest;
import lombok.Data;
import lombok.EqualsAndHashCode;

import javax.validation.constraints.NotBlank;
import javax.validation.constraints.NotNull;
import java.io.Serial;

/**
 * 移除单条黑名单的命令(逻辑删除)。
 *
 * extends BaseRequest 是 BizTemplate.doProcess 类型约束所必需。
 */
@EqualsAndHashCode(callSuper = true)
@Data
public class BlacklistRemoveCommand extends BaseRequest {

    @Serial
    private static final long serialVersionUID = 1L;

    /** 黑名单主键 ID */
    @NotNull(message = "ID 不能为空")
    private Long id;

    /** 操作人(审计) */
    @NotBlank(message = "操作人不能为空")
    private String modifier;
}
