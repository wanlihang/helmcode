# ====================================================================
# 公共默认配置:5 套 env 都会继承本文件,只放与环境无关的"默认值"。
# 禁止 env 专属值(机房/zone/数据源/密钥),env 专属值写到 application-{dev,test,sim,prepub,prod}.properties。
# ====================================================================

# spring 输出颜色(本地终端友好,生产无影响)
spring.output.ansi.enabled=ALWAYS
